TxScript UnlockStream(factory: StreamFactory, id: U256, amount: U256) {
    factory.unlockStream(id, amount)
}

TxScript CancelStream(factory: StreamFactory, id: U256) {
    factory.cancelStream(id)
}
